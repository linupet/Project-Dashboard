# Streamlit theme configuration.
# Forces the dark theme for all users and overrides Streamlit's default
# red accent so primary buttons (the active nav item) appear dark grey.
[theme]
base = "dark"
primaryColor = "#3a3a3a"
